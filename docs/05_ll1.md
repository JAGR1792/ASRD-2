# LL(1)

Una gramatica es LL(1) si no hay interseccion entre conjuntos de prediccion de reglas alternativas del mismo no terminal.

Si hay intersecciones, el parser predictivo no puede decidir con un solo token de lookahead.
